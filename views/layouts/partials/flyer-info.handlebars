<article class=>
      <div class="title">
        <a href="{{flyer_body}}" target="_blank">{{title}}</a>
        <span>({{format_url post_body}})</span>
      </div>
      <div class="meta">
        <a href="/post/{{id}}">{{comments.length}} {{format_plural "comment" comments.length}}</a>
      </div>
    </article>